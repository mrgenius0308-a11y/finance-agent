[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && pip install ."

[deploy]
startCommand = "uvicorn finance_agent.api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
